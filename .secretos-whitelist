# Falsos positivos verificados del escaner de secretos (guardian_sombras).
# Una entrada por linea; se compara como subcadena del texto detectado.

# README.md: bloque de ejemplo en PowerShell que documenta como exportar la
# clave de Groq. El valor es el marcador literal "gsk_...", no una clave.
# Una clave real nunca contiene puntos suspensivos, asi que esta entrada
# no puede enmascarar una fuga autentica.
gsk_...
